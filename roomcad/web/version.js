// The public RoomCAD version. Bumped once for every completed task, so the
// footer always says which build the user is looking at.
// The footer reads this value directly, so there is only one version source.
export const APP_VERSION = "6.7";
